#invitation-modal.modal.fade
  .modal-dialog
    .modal-content
      = form_tag(invite_path, method: :post, class: "form-horizontal") do
        .modal-header
          button.close aria-hidden="true" data-dismiss="modal" type="button"
            | &times;
          h4.modal-title Find & Add New Manager
        .modal-body
          .form-group
            .col-sm-12
              = text_area_tag 'emails', nil, rows: 20, class: 'form-control', placeholder: 'emails'
        .modal-footer
          button.btn.btn-small.btn-primary type='submit' Invite

