.tabbable
  ul.nav.nav-tabs
    li.active
      a href="#sessions" data-toggle="tab"
        |  Sessions
    li
      a href="#pool" data-toggle="tab"
        |  Pool
    li
      a href="#settings" data-toggle="tab"
        |  Settings
  .tab-content
    #sessions.tab-pane.active
      = render 'sessions'
    #pool.tab-pane
      = render 'pool'
    #settings.tab-pane
      = render 'settings'