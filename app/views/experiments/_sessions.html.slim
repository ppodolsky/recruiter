.table-responsive
  table.table.table-condensed.table-borderless.table-hover
    thead
      tr
        th
          | Start at
        th
          | Registred subjects
        th
    tbody
      - @experiment.sessions.each do | session |
        tr id="session_#{session.id}"
          td data-href = "#{experiment_session_path(@experiment, session)}"
            = l session.start_time.to_date
          td data-href = "#{experiment_session_path(@experiment, session)}"
            = session.subjects.count
          td
            .btn-group.pull-right
              = link_to 'Remove', experiment_session_path(@experiment, session), remote: true, method: :delete, :class => 'btn btn-xs btn-danger remove-link', data: { confirm: 'Are you sure?' }